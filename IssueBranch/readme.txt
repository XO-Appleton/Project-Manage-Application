To test this branch and run it on its own, first open your terminal and navigate to the IssueBranch folder in the git repository.
Enter the command "pyhton IssueController.py"
any command except for open, edit, and close will terminate the application (reply is not yet implimented.)
(Using the application will edit the database file and cause differences in branches)